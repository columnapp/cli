{
  "name": "@columnappp/create-column",
  "version": "1.0.0",
  "repository": "https://github.com/columnapp/create-column",
  "scripts": {
    "index": "ts-node index.ts",
    "clean": "rimraf ./dist/",
    "build": "yarn run clean && tsc",
    "prepare": "yarn run build",
    "deploy-local": "yarn run prepare && npm uninstall -g && npm install -g"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": "./dist/index.js",
  "author": {
    "email": "hello@column.app",
    "name": "columnapp",
    "url": "https://column.app"
  },
  "license": "MIT",
  "private": false,
  "dependencies": {
    "colors": "^1.4.0",
    "commander": "^9.4.1",
    "enquirer": "^2.3.6",
    "prompt": "^1.3.0"
  },
  "devDependencies": {
    "@types/prompt": "^1.1.4",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  },
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  }
}
